# The Canary – Speaker Separation & Denoising
# Additional requirements (beyond what's already in venv)

# Core audio ML
speechbrain>=1.0.0          # SepFormer pretrained separation
noisereduce>=3.0.0          # Spectral noise reduction

# Audio I/O (likely already present)
sounddevice>=0.4.6
soundfile>=0.12.1
torchaudio>=2.0.0

# Signal processing
scipy>=1.10.0
numpy>=1.24.0
librosa>=0.10.0

# VAD
silero-vad>=4.0.0

# CLI & display
rich>=13.0.0
typer>=0.9.0

# PyTorch (already in venv)
torch>=2.0.0
