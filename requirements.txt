# The Canary — Dependencies
# Install: pip install -r requirements.txt

# Core ML / Audio
sherpa-onnx>=1.13.0       # ASR engine (SenseVoiceSmall)
numpy>=2.0.0              # Array operations
soundfile>=0.13.0         # WAV file I/O

# Infrastructure
redis>=8.0.0              # State store (sessions, profiles, history)
faiss-cpu>=1.14.0         # Speaker verification index (cosine similarity)

# Agent / MCP
fastmcp>=3.3.0            # MCP server framework (tool decorators)

# UI
rich>=15.0.0              # Terminal UI (demo panels, tables, live display)
